import type { PineGreenPalette } from "../types";
import { PINE_GREEN_PALETTE } from "../pineGreen";

/** Dark theme — pine ramp; override keys here when dark diverges from shared ramp. */
export const darkThemeColors: PineGreenPalette = {
  ...PINE_GREEN_PALETTE,
};
