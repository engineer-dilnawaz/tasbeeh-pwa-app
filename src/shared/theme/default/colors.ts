import type { PineGreenPalette } from "../types";
import { PINE_GREEN_PALETTE } from "../pineGreen";

/** Light theme — pine ramp; override keys here when light needs different stops than dark. */
export const defaultThemeColors: PineGreenPalette = {
  ...PINE_GREEN_PALETTE,
};
