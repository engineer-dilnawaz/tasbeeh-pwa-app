/**
 * Re-export figma-style squircle from `corner-smoothing` for app surfaces
 * (distinct from `@squircle-js/react` used in Button / legacy Squircle wrapper).
 */
export { Squircle as CornerSquircle } from "corner-smoothing";
